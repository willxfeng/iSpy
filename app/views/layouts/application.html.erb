<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href='https://fonts.googleapis.com/css?family=Ubuntu:400,300italic' rel='stylesheet' type='text/css'>

    <title><%= content_for?(:title) ? yield(:title) : "iSpy" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <%= render "layouts/nav" %>

    <div class="row">
      <div class="small-6 small-centered column">
        <div class="flash">
          <% flash.each do |key, value| %>
            <div data-alert class="alert-box round <%= key.to_s == 'notice' ? 'success' : 'alert' %>">
            <%= content_tag :div, value %>
            <a href="#" class="close">&times;</a>
          <% end %>
        </div>
      </div>
    </div>

    <%= yield %>

    <footer class="footer">
      <div class="row">
        <div class="small-12 columns">
          <p class="slogan">iSpy: Street Art in Review</p>
          <p class="links">
            <%= link_to "Home", root_path %>
            <%= link_to "View All Art", arts_path %>
            <%= link_to "Sign In", new_user_session_path %>
            <%= link_to "Add Art", new_art_path %>
          </p>
          <!-- <p class="copywrite">Copywrite not copypwrite © 2015</p> -->
        </div>
      </div>
    </footer>

  </body>
</html>
